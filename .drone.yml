pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/mastodon
        tags: [2.8.1-alpine, latest-alpine]
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
